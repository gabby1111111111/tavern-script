# V0.4 真实环境轻量验收记录

日期：2026-09-08。环境为 SillyTavern 1.18.0、Tavern Helper 4.9.2，Chrome CDP 页面 `https://192.168.1.5:8000/`，视口 1461×867、DPR 1.5。目标脚本 iframe 显示 `V0.4.0-dev`，实时监听开关为启用。

## 超时服务端核对（只读，未重发）

此前真实请求在生图机客户端的 120000ms 截止时间内没有返回。为区分接口故障与响应过慢，本轮没有再次 POST，而是读取现有服务状态和脱敏调用记录：

- `GET /health` 和 `GET /openapi.json` 均快速返回 200；OpenAPI 服务版本为 `chatgpt2api 1.8.0`，存在 `/v1/images/generations`。
- 当前活跃图片配置的非敏感字段为 `gpt-image-2`、`requestMode=auto`、`timeoutMs=120000`；Key、正文、提示词和响应内容未读取或写入报告。
- 服务端应用记录中最新匹配的生成调用为：端点 `/v1/images/generations`、模型 `gpt-image-2`、状态 `success`、耗时 `163439ms`，从 `2026-09-08 06:42:06 UTC` 到 `06:44:50 UTC`。容器图片索引的最新生成时间与该结束时间对齐，说明服务端在客户端截止后完成了图片生成。
- 生图机当前审计仍为任务 `failed`、内存资源数 0、连续快照数 0；迟到的服务端结果没有进入浏览器页面内存。

根因边界：`image-api.ts` 的 `fetchWithTimeout` 从请求准备阶段开始计时，120000ms 后主动中止；本次端点实际需要 163439ms 才成功。因此本次失败是客户端等待上限短于服务端响应时长，尚无证据表明请求格式或路由错误。未修改源代码、未重试、未切换模型或端点。

## 300 秒客户端等待验证（首图，已完成）

- 通过现有设置界面将用户指定局域网 API 配置的 `timeoutMs` 改为 `300000`；当前 schema 允许 `1000–900000`，无需源代码修改。
- 首轮暂用连续剧情画图预设与关闭头像/上一图引用的纯文生图出图预设，并把生成数量临时设为 1；端点、模型和重试次数保持原值。
- 15:15（UTC+8）已在当前聊天发送一条短测试消息。发送后脱敏审计为 `generation=running`、`active_snapshots=1`、`reference_count=0`，尚未重复发送。
- 首图完成后脱敏审计为 `generation=success`、任务 `success`、`cache.artifact_count=1`、`placement_count=1`、`continuity.reference_count=0`。服务端新增记录为 `POST /v1/images/generations`、HTTP 200、`duration_ms=54677`；图片索引计数由 346 增至 347。
- 发送前服务端生成调用记录计数为 181；本轮没有重试。第二轮已切换到开启上一镜头图片参考的连续剧情出图预设，并继续只提交一个图片任务。

## 第二轮正文请求（已停止主动观察）

状态：正文生成仍在途；本轮尚未进入图片任务，连续引用未验证。

- 第二轮已使用连续剧情出图预设，头像参考和上一镜头图片参考均开启；客户端 `timeoutMs=300000`，单次图片数量临时为 1，端点、模型和重试次数未改变。
- 已提交且仅提交一次第二轮短测试消息。停止观察前的脱敏审计为 `run_id=3`、`lifecycle=running`、`generation=running`、任务均为 `idle`、`markers=0`、`cache.artifact_count=1`、`placement_count=1`、`continuity.active_snapshots=0`、`continuity.reference_count=0`、`last_error=null`。
- 当前最新楼层仍是该用户楼层，尚未出现后续助手楼层；浏览器性能记录只有旧失败请求和首图成功请求，没有第二个 `/v1/images/generations` POST。停止按钮不可见；没有声称取消服务端请求，也没有继续轮询或重发。
- 因宿主生成尚未结束，临时连续剧情预设和单次数量暂留在当前设置；待请求自然结束后再按现有设置恢复。在途状态不计为连续引用成功。

## 已通过

- 设置抽屉可以关闭并重新展开；画图预设、出图预设、最近生成、API 四个页签均可切换。切换期间脚本根节点保持 1 个，没有重复挂载。
- 最近生成页在测试前为空。连续剧情示例可由界面添加；测试中选中了新增的画图/出图组合，并确认“上一镜头图片参考”开关生效。
- API 档案实际选中用户授权的局域网端点；Key 只确认已配置，没有读取或记录内容。为控制单任务请求量，测试临时把单次数量改为 1，结束后恢复原值 2。
- 新聊天入口在当前布局不可见，程序点击也没有切换。已保存当前聊天身份后按授权在当前聊天追加两轮测试消息，没有删除或改写旧消息；测试结束仍在原聊天。
- 测试结束恢复原先画图预设、出图预设、API 数量、API 页签和抽屉展开状态。示例预设保留在设置中，未改变原先选择。

## 第一轮旧版运行时结果

两轮正文回复都在页面完成。通过 iframe 内已声明的高层 `getChatMessages('46-49')` 读取原始消息后，确认两个助手楼层各有 1 个有效 `<pic prompt="...">` 标记；DOM 中没有标记是生图机清理标记后的正常表现。两轮审计仍为 `markers.count=0`、任务保持 `idle`，图片 API 端点资源计数为 0，页面图片数为 0，因此有效标记没有进入生图机的 `MESSAGE_RECEIVED` 消费路径，本轮没有调用真实生图 API，也没有得到第一张图，无法继续验证第二轮的上一镜头图片引用。没有输出正文、提示词、Key、图片 URL、Base64 或完整响应。

结束审计为：`run_id=2`，`cache.mode=memory-only`、`persisted=false`、资源和 placement 数均为 0，`continuity.status=released`、活动快照 0、参考来源 0，`last_error=null`。宿主页面的生成状态已结束但旧运行时代码审计仍为 `generation.status=running`、任务 idle。已核对当前 SillyTavern 1.18.0 `script.js`：流式路径先由 `activateSendButtons` 触发 `GENERATION_ENDED`，之后才发送 `MESSAGE_RECEIVED`；结束事件参数是 `chat.length`，不是可靠的最终楼层 id。旧运行时代码在前一个事件中清空 `activeGeneration`，因此后一个合法正文事件被忽略。`last_skip_reason=non-normal-generation` 是后续非正文入口覆盖的状态，不能推断这两轮正文 type。

## 修复后真实顺序验证

实时监听热载入修复后的 `runtime.ts` 后，临时选中连续剧情示例组合并把图片数量设为 1。事件探针只记录脱敏标量，实际顺序为：`GENERATION_STARTED(normal,false)` → `GENERATION_ENDED(52)` → `MESSAGE_RECEIVED(51,normal)` → 两次 `CHARACTER_MESSAGE_RENDERED`；随后有一次独立的 dry-run `GENERATION_STARTED(normal,null,true)`，没有被当成正文图片任务。

修复后第一轮原始助手消息被识别为 1 个有效标记，审计从 `markers.count=1`、`valid_count=1`、任务 `running` 开始，确认结束事件先到时已消费正文并且没有重复启动。用户授权 endpoint 只实际发出 1 个图片任务，最终以 `图片 API 请求超时（120000ms）` 结束；任务为 `failed`，`cache.artifact_count=0`、`continuity.active_snapshots=0`，没有重试，也没有图片产物。控制台未记录目标脚本的 error/warning；未输出 Key、正文、提示词、图片 URL、Base64 或完整响应。

测试后已恢复原画图/出图预设、图片数量 2、API 页签和抽屉展开状态，并移除临时事件监听。连续引用上一图未验，原因是该端点本次真实 POST 未在超时内返回图片。

CDP 采样到的控制台事件以宿主调试输出为主；目标脚本前缀相关的 error/warning 数为 0。采样中另有 4 条未归属脚本的浏览器 Log error 和 27 条 warning，未记录原文，不能据此判定为生图机故障。

## 用户下一步最小操作

客户端等待已按授权设为 `300000ms`，首轮单图已通过；第二轮正文请求仍在途，尚未产生第二个图片 POST，因此 `continuity.reference_kinds` 和参考数量尚未验收。不要再次提交或重试，也不要把当前在途状态当作成功；待宿主自然结束后再读取现有审计，并在流程结束时恢复临时数量和预设。
